# Quality gates — must pass before any commit lands.
# Order: lint/format (staged) -> format:check (whole tree) -> view-line discipline
#        -> feature-coverage map -> unit tests w/ coverage -> CRAP -> build
npx lint-staged
npm run format:check
npm run check:lines
npm run check:features
npm run test:coverage
npm run crap
npm run build
