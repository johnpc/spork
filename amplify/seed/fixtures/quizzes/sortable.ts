/** SORTABLE fixture — STUB (replaced by the SORTABLE build). DATA (gate-exempt). */
import type { QuizFixture } from './types';

export const sortableQuiz: QuizFixture = {
  topic: 'SORTABLE (stub)',
  categorySlug: 'miscellaneous',
  description: 'Placeholder SORTABLE quiz.',
  mode: 'SORTABLE',
  inputMode: 'ARRANGE',
  scoringMode: 'BUCKETING',
  timeLimitSeconds: 120,
  answers: [{ display: 'Placeholder', accepted: ['placeholder'], promptKind: 'NONE' }],
};
