/** CLICKABLE fixture — STUB (replaced by the CLICKABLE build). DATA (gate-exempt). */
import type { QuizFixture } from './types';

export const clickableQuiz: QuizFixture = {
  topic: 'CLICKABLE (stub)',
  categorySlug: 'miscellaneous',
  description: 'Placeholder CLICKABLE quiz.',
  mode: 'CLICKABLE',
  inputMode: 'CLICK',
  scoringMode: 'MEMBERSHIP',
  timeLimitSeconds: 120,
  answers: [{ display: 'Placeholder', accepted: ['placeholder'], promptKind: 'NONE' }],
};
